{
  "name": "fluid",
  "version": "0.1.0",
  "version_short": "0.1",
  "homepage": "https://boundarystudio/fluid",
  "author": {
    "name": "Ashfahan",
    "email": "me@ashfahan.com",
    "url": "https://ashfahan.com"
  },
  "maintainers": [
    "Ashfahan"
  ],
  "description": "Modern, Stylish, Easier and Powerful front-end framework for faster web development.",
  "style": "dist/fluid.css",
  "scss": "src/fluid.scss",
  "main": "dist/fluid.css",
  "keywords": [
    "css",
    "scss",
    "mobile-first",
    "mobile-ready",
    "responsive",
    "front-end",
    "web",
    "framework",
    "flexbox"
  ],
  "contributors": [
    "Boundary Studio"
  ],
  "bugs": {
    "url": "https://github.com/BoundaryStudio/fluid/issues/"
  },
  "scripts": {
    "build": "npm-run-all -s build:*",
    "build:clean": "rimraf dist",
    "build:compile": "node-sass ./src --output ./dist/ --output-style expanded --indent-type space --indent-width 2 --source-map true",
    "build:prefix": "postcss ./dist/*.css --dir ./dist --use autoprefixer --map false",
    "build:minify": "foreach -g ./dist/*.css -i ./dist/*.min.css -x \"node-sass ./dist/#{name}.css ./dist/#{name}.min.css --output-style compressed --source-map true\"",
    "deploy": "yarn run build",
    "start": "yarn run build:compile -w"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/twbs/bootstrap.git"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "foreach-cli": "^1.8.1",
    "autoprefixer": "^9.6.0",
    "node-sass": "^4.12.0",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^6.1.2",
    "rimraf": "^2.6.3"
  },
  "engines": {
    "node": ">=8"
  },
  "files": [
    "dist/{css,js}",
    "src/{scss}"
  ]
}
